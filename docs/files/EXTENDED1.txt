
function xTable(data)
  local mt = {
    __index = table
  }
  return setmetatable(data, mt)
end

fruits = xTable{ "apple", "banana", "lemon", "orange" }

fruits.insert(fruits, "pear")

fruits:insert("melon")

print( #fruits, fruits:unpack() )   -- 6    apple    banana    lemon    orange    pear    melon
