
--[[
function touch_start(num_detected)
    for i = 0, num_detected -1 do
        local toucher = ll.DetectedKey(i)
        print(ll.DetectedName(i))
    end
end
]]


ll = llcompat

function LLEvents.touch_start(num_detected)
    num_detected = #num_detected
    for i = 0, num_detected -1 do
        local toucher = ll.DetectedKey(i)
        print(ll.DetectedName(i))
    end
end
